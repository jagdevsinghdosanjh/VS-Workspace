let x=1 
//Use of let keyword does not allow to declare one variable again with different values.
console.log(x)
let x1=2
console.log(x1)
//Even if we try to declare same variable (declared with let keyword) using var it will through an error.
let x2=3
console.log(x2)